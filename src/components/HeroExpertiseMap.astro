<div
  class="expertise-orbit"
  role="img"
  aria-label="Quatre expertises gravitent autour du collectif : développement web, intelligence artificielle et données, sécurité applicative, ERP et API."
>
  <div class="expertise-orbit__texture" aria-hidden="true"></div>
  <div
    class="expertise-orbit__orbit expertise-orbit__orbit--wide"
    aria-hidden="true"
  >
  </div>

  <div class="expertise-orbit__center" aria-hidden="true">
    <span class="material-symbols-outlined">waves</span>
  </div>

  <div class="expertise-orbit__item expertise-orbit__item--web">
    <span
      class="expertise-orbit__icon material-symbols-outlined"
      aria-hidden="true">code</span
    >
    <span class="expertise-orbit__label">Développement web</span>
  </div>

  <div class="expertise-orbit__item expertise-orbit__item--data">
    <span
      class="expertise-orbit__icon material-symbols-outlined"
      aria-hidden="true">database</span
    >
    <span class="expertise-orbit__label">IA &amp; données</span>
  </div>

  <div class="expertise-orbit__item expertise-orbit__item--security">
    <span
      class="expertise-orbit__icon material-symbols-outlined"
      aria-hidden="true">shield_lock</span
    >
    <span class="expertise-orbit__label">Sécurité applicative</span>
  </div>

  <div class="expertise-orbit__item expertise-orbit__item--systems">
    <span
      class="expertise-orbit__icon material-symbols-outlined"
      aria-hidden="true">account_tree</span
    >
    <span class="expertise-orbit__label">ERP &amp; API</span>
  </div>
</div>

<style>
  .expertise-orbit {
    position: relative;
    width: 100%;
    max-width: 35rem;
    aspect-ratio: 1.04;
    margin-inline: auto;
    isolation: isolate;
  }

  .expertise-orbit__texture {
    position: absolute;
    inset: 2%;
    z-index: -2;
    background-image: repeating-linear-gradient(
      135deg,
      color-mix(in srgb, var(--color-primary) 5%, transparent) 0,
      color-mix(in srgb, var(--color-primary) 5%, transparent) 1px,
      transparent 1px,
      transparent 8px
    );
    mask-image: radial-gradient(ellipse at center, black 12%, transparent 72%);
  }

  .expertise-orbit::before {
    position: absolute;
    top: 50%;
    left: 50%;
    z-index: -1;
    width: 42%;
    height: 42%;
    transform: translate(-50%, -50%);
    border-radius: 50%;
    background: color-mix(in srgb, var(--color-primary-fixed) 58%, transparent);
    filter: blur(2.8rem);
    content: "";
  }

  .expertise-orbit__orbit {
    position: absolute;
    top: 50%;
    left: 50%;
    z-index: -1;
    border: 1px solid color-mix(in srgb, var(--color-primary) 17%, transparent);
    border-radius: 50%;
  }

  .expertise-orbit__orbit--wide {
    width: 72%;
    height: 72%;
    transform: translate(-50%, -50%);
  }

  .expertise-orbit__center {
    position: absolute;
    top: 50%;
    left: 50%;
    display: grid;
    width: clamp(5.8rem, 19%, 6.8rem);
    aspect-ratio: 1;
    transform: translate(-50%, -50%);
    place-items: center;
    border: 1px solid color-mix(in srgb, var(--color-primary) 24%, transparent);
    border-radius: 1.4rem;
    background: var(--color-surface-container-lowest);
    color: var(--color-primary);
    box-shadow:
      0 1.2rem 2.8rem color-mix(in srgb, var(--color-primary) 12%, transparent),
      0 0 2.5rem
        color-mix(in srgb, var(--color-primary-fixed-dim) 42%, transparent);
  }

  .expertise-orbit__center > .material-symbols-outlined {
    font-size: 2.15rem;
  }

  .expertise-orbit__item {
    position: absolute;
    width: 3.75rem;
    height: 3.75rem;
    transform: translate(-50%, -50%);
  }

  .expertise-orbit__item--web {
    top: 24.5%;
    left: 24.5%;
  }

  .expertise-orbit__item--data {
    top: 24.5%;
    left: 75.5%;
  }

  .expertise-orbit__item--security {
    top: 75.5%;
    left: 24.5%;
  }

  .expertise-orbit__item--systems {
    top: 75.5%;
    left: 75.5%;
  }

  .expertise-orbit__icon {
    position: relative;
    z-index: 1;
    display: grid;
    width: 100%;
    height: 100%;
    flex: none;
    place-items: center;
    border: 1px solid
      color-mix(in srgb, var(--color-outline-variant) 48%, transparent);
    border-radius: 50%;
    background: color-mix(
      in srgb,
      var(--color-surface-container-lowest) 94%,
      transparent
    );
    color: var(--color-primary);
    font-size: 1.45rem;
    box-shadow: 0 0.75rem 1.3rem
      color-mix(in srgb, var(--color-primary) 10%, transparent);
  }

  .expertise-orbit__label {
    position: absolute;
    top: 50%;
    left: calc(100% - 0.55rem);
    transform: translateY(-50%);
    padding: 0.52rem 0.72rem 0.52rem 0.9rem;
    border: 1px solid
      color-mix(in srgb, var(--color-outline-variant) 42%, transparent);
    border-radius: 0 0.7rem 0.7rem 0;
    background: color-mix(
      in srgb,
      var(--color-surface-container-lowest) 92%,
      transparent
    );
    color: var(--color-primary);
    font-family: var(--font-headline);
    font-size: clamp(0.7rem, 1.4vw, 0.82rem);
    font-weight: 700;
    line-height: 1.1;
    white-space: nowrap;
    box-shadow: 0 0.55rem 1.2rem
      color-mix(in srgb, var(--color-primary) 8%, transparent);
  }

  .expertise-orbit__item--web .expertise-orbit__label,
  .expertise-orbit__item--security .expertise-orbit__label {
    right: calc(100% - 0.55rem);
    left: auto;
    padding: 0.52rem 0.9rem 0.52rem 0.72rem;
    border-radius: 0.7rem 0 0 0.7rem;
  }

  @media (max-width: 430px) {
    .expertise-orbit {
      aspect-ratio: 0.96;
    }

    .expertise-orbit__orbit--wide {
      width: 62%;
      height: 62%;
    }

    .expertise-orbit__item--web,
    .expertise-orbit__item--data {
      top: 28%;
    }

    .expertise-orbit__item--security,
    .expertise-orbit__item--systems {
      top: 72%;
    }

    .expertise-orbit__item--web,
    .expertise-orbit__item--security {
      left: 28%;
    }

    .expertise-orbit__item--data,
    .expertise-orbit__item--systems {
      left: 72%;
    }

    .expertise-orbit__icon {
      font-size: 1.2rem;
    }

    .expertise-orbit__item {
      width: 3rem;
      height: 3rem;
    }

    .expertise-orbit__label,
    .expertise-orbit__item--web .expertise-orbit__label,
    .expertise-orbit__item--security .expertise-orbit__label {
      top: calc(100% + 0.35rem);
      right: auto;
      left: 50%;
      max-width: 7rem;
      padding: 0.4rem 0.6rem;
      border-radius: 0.7rem;
      font-size: 0.65rem;
      text-align: center;
      transform: translateX(-50%);
      white-space: normal;
      width: max-content;
    }

    .expertise-orbit__item--web .expertise-orbit__label,
    .expertise-orbit__item--data .expertise-orbit__label {
      top: auto;
      bottom: calc(100% + 0.35rem);
    }
  }
</style>
